import scala.concurrent.Future
import scala.concurrent.ExecutionContext.Implicits.global

import scala.util.{Failure, Success}

object ForFutureTry {

  //  val res = Future{
  //    Thread.sleep(100) ;
  //    if (Math.random * 100 < 50) Success("user found") else Failure(UserNotFound)
  //  }






}
